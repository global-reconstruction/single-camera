# Single-camera un-calibrated global reconstruction

# Collaboration

* All code should be formatted with `clang-format` (using default `LLVM` style)
* CMakeLists should be formatted with `cmake-format`

